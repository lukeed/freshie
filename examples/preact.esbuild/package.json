{
  "private": true,
  "scripts": {
    "build": "freshie build"
  },
  "dependencies": {
    "preact": "10.4.8"
  },
  "devDependencies": {
    "@freshie/plugin.esbuild": "workspace:*",
    "@freshie/ui.preact": "workspace:*",
    "freshie": "workspace:*",
    "esbuild": "0.8.0"
  }
}
