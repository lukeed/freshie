{
  "private": true,
  "scripts": {
    "build": "freshie build"
  },
  "dependencies": {
    "httpie": "next",
    "preact": "10.4.8"
  },
  "devDependencies": {
    "esbuild": "0.7.7",
    "@freshie/plugin.esbuild": "workspace:*",
    "@freshie/ui.preact": "workspace:*",
    "freshie": "workspace:*"
  }
}
