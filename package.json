{
  "private": true,
  "version": "0.0.2",
  "repository": "lukeed/freshie",
  "license": "MIT",
  "scripts": {
    "build": "node build",
    "pretest": "tsc --noEmit --skipLibCheck",
    "test": "uvu -r ts-node/register packages __tests__ -i fixtures"
  },
  "engines": {
    "node": ">=10"
  },
  "devDependencies": {
    "ts-node": "10.2.1",
    "typescript": "4.4.3",
    "uvu": "0.5.1"
  }
}
